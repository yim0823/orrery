# Synthetic. Included because the engine gets this one wrong.
id: INC-0003
title: "Payment provider outage; checkout degraded but kept taking orders"
occurred_at: "2026-06-21T19:15:00+09:00"
world: world.yaml
trigger: ext-payments
event: down
elapsed_s: 600      # ten minutes in; the retry queue had room
observed:
  svc-checkout: degraded   # queued payments and kept the cart working
  svc-web: degraded
  svc-inventory: up
notes: |
  Checkout treats the payment provider as a soft dependency: it queues and retries.
  Paired with INC-0004, which is the same failure left running four hours.
